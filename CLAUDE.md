# CLAUDE.md

## このリポジトリについて

「やりたいことリスト100」の作り直し。**実装は未着手**で、現在あるのは仕様と技術構成のドキュメントのみ。

読む順番: `PRODUCT_SPEC.md`（何を作るか）→ `TECH_STACK.md`（どう作るか・なぜそう決めたか）
→ `MEMO.md`（申し送り）。旧実装の情報は `docs/legacy-spec.md`。

## 前提

**このコードベースは人間がレビューしない。** コードを書くのも保守するのも AI である。
したがって以下が通常より重要になる。

- **テストが唯一の安全網。** 特に認可のテストは仕様として書く
- **ローカルで全部動くこと。** `wrangler dev` + ローカル D1 + Vitest/Miniflare で
  赤/緑の信号が数秒で返る状態を保つ
- **コンソールでしか設定できない項目はリポジトリに書き残す。**
  設定が原因の不具合はコードを読んでも原因に到達できない

## 不変条件

実装時に守る。破る場合はドキュメントを先に更新すること。

- **クライアントから DB を直接触らせない。** 認可はサーバーのコード1箇所に集める
- **公開 URL の ID は推測不可能な値。** 連番 ID を露出させない
- **編集用 `listId` と公開用 `shareId` は別の値。**
  `shareId` を再生成すると共有リンクが無効になる、という仕様のため
- **非公開判定は必ずサーバー側。OGP 経路も同じ認可を通す**（画像だけ漏れるのが典型的な事故）
- **画像生成サービスに渡すデータは、呼び出し側で認可を通した後のものに限る。**
  生成サービス側に公開/非公開を判断させない
- **画像生成エンドポイントは HMAC で署名する。** 誰でも叩ける状態にしない
- **セッションはサーバー側から失効できる方式にする。** ストアは D1 のみ
- **セッション Cookie に `Domain` 属性を付けない**（host-only）。
  `workers.dev` は他人のワーカーとサブドメインを共有する空間のため
- **入力検証は Zod で1箇所に集約する。** 文字数上限もそこで効かせる

## 作業上の注意

- **無料枠やライブラリの状況は 2026-08-05 時点の調査。**
  実装に入る前に `TECH_STACK.md` §13 の内容を再確認する
- 仕様や構成を変える判断をしたら、**`PRODUCT_SPEC.md` / `TECH_STACK.md` を更新する。**
  両ドキュメントには「決定事項」と「未決の論点」の表があるので、そこに反映する
- 変数名に `bucket` を単独で使わない（R2 のバケットと紛らわしい）
