# 技術構成の検討

`PRODUCT_SPEC.md` の仕様を実現するための技術選定。検討の経緯と、各案のメリット・リスクを残す。

無料枠やライブラリの状況はいずれも変わりうる。**着手時に §13 の調査メモを再確認すること。**

---

## 1. 前提と評価軸

### 制約
- 個人開発。**月100円以下が理想**、$5 は許容範囲だが避けたい
- 広く公開するため**セキュリティは担保する**
- **コードは人間が書かず、AI が書いて保守する**（＝人間によるコードレビューが存在しない）
- DB は SQL / NoSQL どちらでもよい。既存資産に合わせる制約はない
- **SNS カードは共有時に目を引く要素なので、作り込めるようにする**（§3）

### 「AI が保守しやすい」条件（効く順）
1. **ローカルで動かして赤/緑の信号が数秒で返るか** — 最重要
2. **境界を越えるときに型が効くか**（DB ↔ サーバー ↔ クライアント）
3. **暗黙のルールが少ないか**
4. **概念の数が少ないか**
5. 学習データの量

5 を最初に考えがちだが、実際に事故を防ぐのは 1〜3。
そして人間がレビューしないなら、**レビューの代わりになるのはテストだけ**なので、
テストが安く速く書ける環境かどうかが実質的な安全性を決める。

**もう1つ見落としやすい条件がある。設定がコードに乗っているか。**
コンソールでクリックして設定した項目が原因の不具合は、AI がコードをいくら読んでも原因に到達できない。
コード管理できない設定は、リポジトリに手順として書き残す必要がある（§8 参照）。

### この軸で Next.js App Router が不利な理由
- 暗黙のルールが多く、間違えても実行時まで分からない（サーバー/クライアント境界、`"use client"`、
  キャッシュの再検証、RSC のシリアライズ制約）
- **壊れ方が型チェックに現れない。** `"use client"` を書いたファイルでサーバー専用の API を呼ぶ、
  拡張子が `.ts` のまま JSX を返そうとする、といった矛盾が実行時まで表面化しない
- RSC のテストが書きにくい → テストを安全網にする戦略と相性が悪い
- 仕様変更が速く、AI の知識がインストール済みバージョンとずれる

---

## 2. コストの正体

**費用が発生する理由は画像のラスタライズ（CPU）だけ。** DB・ホスティング・認証はすべて無料枠で足りる。

Cloudflare Workers の無料プランは1リクエストあたりの CPU 時間が 10ms 程度に制限されている。
SVG のラスタライズは CPU を数百ms 使うため、これを超える。有料プラン（$5/月）なら数十秒まで緩む。

したがって選定の核心は **「画像のラスタライズをどこでやるか」** の一点。

---

## 3. OGP と画像出力の方針（決定）

### 2種類の画像を作る（中身は別）

| | 中身 | 用途 | 大きさ |
|---|---|---|---|
| **OGP 画像** | **リストのタイトルと達成状況** | SNS カード | 横長・固定（1200×630 など） |
| **ダウンロード画像** | **すべてのやりたいこと** | ユーザーが保存・投稿する | **横長・固定**（複数列に並べる） |

**この2つは見た目が違う。**OGP に100項目は入らないし、入れる必要もない。
共有された相手に「何のリストで、どこまで進んでいるか」が一目で伝わればよい。
一覧を見せるのはダウンロード画像の役割。

### OGP 画像は動的生成にする

**SNS カードは共有時に目を引く要素であり、体験の入口になるため作り込む。**

> 検討の記録: 「タイトルだけなら `og:title` のテキストで表示されるので画像は静的1枚で足りる」
> という案も検討した。SNS のカードは `og:image` と `og:title` / `og:description` を別々に扱うため、
> 情報としては伝わる。ラスタライザが不要になり ¥0 でデプロイ先1つに収まる利点があった。
> ただし**カードの見栄えを捨てることになる**ため採用しない。
> 予算やCPUの制約で行き詰まった場合の**最小構成の退避先**として記録しておく。

将来的にユーザー側でカスタマイズできる余地を残す（何をカスタマイズ可能にするかは未決。§12）。

### 更新は URL バージョニングで反映する

生成タイミングは**閲覧時ではなく更新時**。タイトル変更・項目編集・公開切替などのイベントで生成し、
以降は保存済みを配る。手動更新ボタンを置くこともできる。

**SNS 側は `og:image` を積極的にキャッシュするため、画像を更新しても URL が同じだと反映されない。**
対策として **URL に更新日時を含める**。

```
/og/:id?v=<updatedAt>
```

`og:image` のメタタグにこの URL を出力すれば、更新後は別 URL として扱われ、再取得される。

### 画像出力も同じ「仕組み」に載せる（デザインは別）

**共通にするのはラスタライズのパイプライン。レイアウトは2つの別テンプレートになる。**

- 共通: 実行場所（Deno Deploy）、ラスタライザ、**日本語フォントの読み込み**、署名付き呼び出しの作法
- 別々: レイアウト定義（OGP 用の横長カード / ダウンロード用の縦長一覧）

**共通化の利点はフォントとビルド設定を1回で済ませられること**であって、
デザインが一致することではない。この2つの見た目は違う。

画像出力をクライアント側（canvas）で作る案もあるが、サーバー側に寄せる理由が3つある。

- **フォントの作業がどちらにしても必要。**OGP を動的生成する時点でサーバーに日本語フォントを
  載せるので、ダウンロード画像を別方式にしても手間は減らない
- **ブラウザ差異（特に Safari）を踏まなくて済む**
- **スナップショットテストが書ける。**クライアント側の canvas 描画は検証が難しく、
  人間がレビューしない前提では自動で確認できる方を採りたい

CPU 枠への影響は小さい。ダウンロード画像はユーザー操作による低頻度アクセスで、
月15 CPU 時間の枠に対して十分収まる。

---

## 4. 画像生成の実装方針

### 4.1 ラスタライズをどこでやるか

| | 実行場所 | 費用 | 画像の鮮度 |
|---|---|---|---|
| **① Deno Deploy 無料枠** | サーバー | ¥0 | 常に最新 |
| ② Cloudflare Workers Paid | サーバー | $5/月 | 常に最新 |
| ③ ブラウザで生成してアップロード | 持ち主の端末 | ¥0 | **古くなりうる** |

**① を採用する。** アプリ本体は Cloudflare Workers に置き、**画像生成だけ Deno Deploy に置く。**

Deno Deploy の無料枠は「1リクエストあたり 50ms CPU」から**月間 15 CPU 時間のプール制**に変更されており、
無料枠のままラスタライズが通る。

この分割は自然な継ぎ目に沿っている。**画像生成は「データ → PNG」の純関数**であり、
DB も認証状態も持たない。渡すデータに HMAC で署名すれば、共有する秘密は鍵1つで済む。

なぜ全部を Deno Deploy に置かないか:
- **リージョンが単一で日本にない**（US/EU）。アプリ本体を置くと操作レイテンシが悪化する。
  画像は CDN でキャッシュして配るので、生成だけなら影響しない
- 有料プランが **$20/月**（Cloudflare Workers の $5 より高い）。スケール時の逃げ道が高い

③ は2つのリスク（画像が古くなる／ユーザーのアップロードを自分のドメインで配る）を抱えるため採用しない。

### 4.2 日本語フォントの扱い

**なぜ問題になるか。** サーバーには OS のフォントがない。ブラウザは端末のフォントを使えるが、
サーバーで文字を画像に描くには**フォントファイルのバイト列を実行環境に持ち込む必要がある**。
日本語は漢字が多く、Noto Sans JP のフルセットは数MB になる。

| | 内容 | 評価 |
|---|---|---|
| **フルセット同梱** | フォントファイルをデプロイに含める | **採用** |
| 静的サブセット | 使う文字だけ事前に抽出しておく | **今回は使えない** |
| 動的サブセット | リクエストごとに必要な文字だけ外部から取得 | 依存が増える |

**静的サブセットは使えない。** ユーザーが入力した任意の日本語を描くため、
どの文字が登場するか事前に分からない。
（ブログの OGP でよく使われる手だが、あれは「ビルド時に決まった文字列だけ描く」ケース）

**フルセット同梱を採用する理由**
1. **Deno Deploy には Workers 無料枠の 3MB のような厳しいスクリプトサイズ上限がない。**
   これは画像生成を Deno Deploy に置いたことの副次的な利点
2. 外部サービスへの依存が増えない ＝ AI が保守するときに壊れる箇所が少ない
3. パースはコールドスタート時のみで、以降はメモリに保持できる

**実装上の注意**
- **使うウェイトを最小限にする**（Regular と Bold の2つまで）。ウェイトの数だけサイズが乗る
- **ライセンスを確認する。**同梱は再配布に当たるため、SIL Open Font License のフォント
  （Noto Sans JP など）を選ぶ

### 4.3 画像生成ライブラリ

| | 入力 | CSS 対応 | 学習データ | 判定 |
|---|---|---|---|---|
| **Satori + @resvg/resvg-wasm** | JSX 風オブジェクト | flexbox のサブセット。grid 不可 | **最多** | **採用** |
| Takumi | — | 広い。性能も良いとされる | 少ない | 次善（Satori で詰まったら） |
| HTML → PNG の WASM レンダラ | HTML + Tailwind CSS | Tailwind をそのまま | 少ない | 保留 |
| ヘッドレスブラウザ | HTML | 完全 | 多い | 却下（無料枠で非現実的） |
| canvas を自分で描く | 命令的 | なし（自分で計算） | 多い | 却下（レイアウトを自前実装） |

**Satori を採用する理由**
- **学習データが最多**で、AI が書ける・直せる。今回の最重要基準（§1）に直接効く
- **Deno Deploy でも Cloudflare Workers でも動く。**
  ネイティブ依存がなく JS だけで動くため、¥0 案から $5 案へ移行してもレンダラを変えずに済む
- 出力が決定論的で、**スナップショットテストが書ける**

**デメリットと対処**
- CSS の対応範囲が狭い（flexbox のサブセットのみ、grid 不可）。
  ただし今回のデザインは単純（OGP は横長カードにタイトルと数値、
  ダウンロードは行の繰り返し）なので、**「対応範囲内でデザインする」制約を最初から受け入れる**
- エッジ環境では Node 版ではなく `@resvg/resvg-wasm` を使う
- **WASM はモジュール起動時に1回だけロードする。**リクエストごとに読むと CPU を無駄に使う
- **4列×25行のグリッドは flexbox で組める**（横に4つの列、各列に25行を縦積み）。
  grid を使わない構成なので Satori の対応範囲に収まる
- **背景の繰り返しパターンは避ける。**`background-repeat` の対応が怪しいため、
  装飾を入れるなら**あらかじめ全面サイズの画像を1枚用意して敷く**方が確実

---

## 5. プラットフォームの選択肢比較

| | 月額 | 商用利用 | デプロイ先 | 最大のリスク |
|---|---|---|---|---|
| **A. Cloudflare + Deno Deploy（画像生成のみ）** | ¥0 | 可 | 2つ | デプロイ先が2つ |
| B. Cloudflare Workers Paid で完結 | $5 | 可 | 1つ | 目標超過 |
| C. Vercel Hobby + Neon | ¥0 | **不可** | 1つ | 収益化時に $20/月 |
| D. Firebase（Firestore 中心） | ¥0 | 可 | 1つ | セキュリティルールが唯一の防壁 |
| E. Supabase | ¥0 | 可 | 1つ | 7日無操作で停止 |
| F. 全部 Deno Deploy | ¥0 | 可※ | 1つ | 日本からのレイテンシ、Pro が $20 |

※ 無料枠は "personal use and smaller projects" 向けと説明されている。明示的な商用禁止の記載は見つからないが、
着手時に規約を確認すること

---

## 6. 各案のメリット・リスク

### A. Cloudflare + Deno Deploy（画像生成のみ） ← 推奨

**メリット**
- **¥0 で、画像が常に最新**。ユーザーの端末に依存しない
- アプリ本体は東京リージョンで動くため操作が速い
- 画像生成は純関数として切り離されており、DB も認証も持たない。**壊れても本体に波及しない**
- OGP と画像出力でレンダラを共用できる
- 商用利用の制限がない
- D1 を含めてローカルで全部動く（AI が検証できる）
- ネットワークレベルの DDoS 対策が付いてくる。レート制限は `ratelimit` バインディングで実装できる
  （WAF はカスタムドメインが必要。§8）
- 詰まったら B（$5）に寄せられる。**移行は生成コードの置き場所を変えるだけ**

**リスク**
- **デプロイ先が2つになる**。環境変数が2箇所、ログが2箇所。
  ただし2つ目は状態を持たない単一エンドポイントで、共有するのは HMAC 鍵1つ
- Deno Deploy の無料枠（月100万リクエスト / CPU 15時間 / 帯域 20GB）を超えると課金。
  画像は CDN でキャッシュするため実際の生成回数は更新時のみ
- ビルド時間も CPU 枠を消費する
- クレジットカード登録をしないと制限が厳しくなるとの記述がある
- **日本語フォントの扱いが必要**（サブセット化など）
- Hono の学習データは Next.js より少ない

### B. Cloudflare Workers Paid（$5/月）で完結

**メリット**
- デプロイ先が1つのまま、サーバー側生成ができる。最も単純
- CPU 制限を気にしなくてよく、設計の自由度が上がる
- HMAC 署名によるサービス間通信が不要になる

**リスク**
- 目標の100円を超える
- **A → B の移行は安い**ので、A で始めて問題が出たら上げればよい

### C. Vercel Hobby + Neon

**メリット**
- Next.js が素で動き、`next/og` で画像生成が最も楽。¥0
- 学習データが最も多い

**リスク**
- **商用利用不可**。収益化した時点で $20/月 で、Cloudflare の $5 より高い
- Next.js App Router の暗黙ルール問題（§1）が残る

### D. Firebase（Firestore 中心）

**メリット**
- **Firebase Auth が強い**（§8 で別途検討）
- Spark プランはカード不要で ¥0
- 端末間のリアルタイム同期が無料で付く

**リスク**
- **セキュリティルールが唯一の防壁**。クライアントが直接 DB を叩くモデルなので、
  ルールを1行間違えると全データが露出する。**人間がレビューしない前提と最も相性が悪い**
- **集計が弱い**。「取り入れ数ランキング」はカウンタを自前管理することになり整合性バグの温床。
  Postgres/SQLite なら1本のクエリで済む
- リスト数上限5つ・重複取り入れ防止といった制約を DB に任せられず、アプリコードで守ることになる
- **Cloud Functions が Blaze（従量課金）必須**。カード登録が必要で上限なしの課金リスクを負う
  （予算アラートは通知するだけで停止しない）
- 無料枠の読み取り 50k/日 は、取り入れ面を作ると意外に消費する
- クエリ制約が強く、AI が書いたクエリが実行時にインデックスエラーになりやすい

**結論: DB としては採用しない。認証だけは §8 で検討する。**

### E. Supabase

**メリット**
- Postgres なので制約・集計が使える。Auth も込み。RLS で DB レベルの認可

**リスク**
- **無料プロジェクトは7日間アクセスがないと一時停止する**。公開直後の閑散期に止まるため、
  公開アプリとしては厳しい
- RLS の設定ミスリスクは Firestore と同種

### F. 全部 Deno Deploy

**メリット**
- デプロイ先が1つで、サーバー側生成、¥0 が同時に成立する
- Hono がそのまま動く

**リスク**
- **リージョンが単一で日本にない**。アプリ操作のレイテンシが悪化する
- 有料プランが $20/月。Cloudflare の $5 より逃げ道が高い
- プラットフォームが移行途中（Classic は 2026年7月20日に廃止）。仕様変更の頻度が高く、
  AI の知識がずれやすい
- D1 の代わりに Deno KV を使うと SQL の制約が使えなくなる（Neon や Turso を併用すれば回避可能）

レイテンシを許容できるなら「デプロイ先1つ」の利点は大きい。**A の次に検討する価値がある。**

---

## 7. 推奨構成

**A（Cloudflare 無料 + Deno Deploy で画像生成）で ¥0。**
画像生成が問題になったら B（$5）に寄せる。

| 層 | 選択 |
|---|---|
| ランタイム（アプリ本体） | Cloudflare Workers |
| ルーティング・API | Hono + Zod バリデータ |
| 公開ページ・OGP メタの SSR | `hono/jsx`（サーバー側 JSX。React 不要） |
| アプリ本体の UI | React + Vite の SPA（Workers から静的配信） |
| DB | D1（SQLite）+ Drizzle |
| 型の連結 | Hono RPC（`hc<AppType>`）でサーバーの型がクライアントに流れる |
| 画像レンダラ | **Deno Deploy 上の単一エンドポイント**（OGP・画像出力で共用） |
| 画像のキャッシュ | Cloudflare CDN（＋必要なら KV） |
| OGP のキャッシュ破棄 | **URL に更新日時を含める**（`/og/:id?v=<updatedAt>`） |
| 認証 | Better Auth（§8。ダメなら Firebase Auth + 自前 DB セッション） |
| エラー通知 | Sentry の無料枠など |

### URL の役割分担

| URL | 内容 | 認証 | 描画 |
|---|---|---|---|
| `/` | 未ログイン: ローカルのリスト / ログイン: **最後に更新したリスト** | 不要 | SPA |
| `/lists` | マイリスト一覧（作成・切替・リネーム・削除） | 要 | SPA |
| `/lists/:listId` | 個別のリスト編集 | 要 | SPA |
| `/discover` | 取り入れ面（将来） | 不要（取り入れ操作のみ要） | SPA |
| `/share/:shareId` | 共有公開ページ（OGP メタタグ付き） | 不要 | **SSR** |
| `/og/:shareId?v=` | OGP 画像（Deno Deploy に署名付きで生成を依頼、CDN でキャッシュ） | 不要 | 画像 |
| `/export/:listId` | ダウンロード画像 | 要 | 画像 |
| `/api/*` | JSON | 混在 | — |

**`/` で一覧を見せず、最後に更新したリストを直接開く。**
リストを1つしか持っていない人に無駄な1タップを作らないため。

### 編集用 ID と公開用 ID を分ける

**`listId`（編集用・内部）と `shareId`（公開用）は別の値にする。**

- **`shareId` を再生成するだけで共有リンクを無効化できる。**
  「うっかり送った相手に見られ続ける」状態から抜ける手段になる
- 公開 URL から編集 URL が推測できない（認可で守られてはいるが、面が分かれている方が事故りにくい）
- どちらも推測不可能な値にする。連番 ID は露出させない

### 認証のコールバック

Better Auth を使うとコールバックは `/api/auth/callback/...` に収まるため、
**コールバック用のユーザー向け画面は作らない**。
localStorage からの引き継ぎ処理はログイン後の `/` で行う。

Next.js が1つのファイル体系に混ぜているものが物理的に分かれるため、
**AI が「今どの実行環境のコードを書いているか」を間違えられない**構造になる。
同一オリジンなので Cookie も CORS も問題にならない。

### なぜ BE を分けないか（アプリ本体の話）

**フロントと API を別ドメインに置くと、Cookie と CORS の設定が両方とも必要になる。**
`SameSite=None; Secure` の Cookie が必須になり、ローカル開発まで https を要求され、
CORS の origin 設定は末尾スラッシュの有無まで一致していないと認証が通らない。
**どれも型でもテストでも捕まえにくい**ため、人間がレビューしない前提では踏みたくない類の設定ミスになる。

同一オリジンなら Cookie は `SameSite=Lax` で済み、CORS 設定自体が不要になる。

画像生成だけを別サービスに置くのはこれと矛盾しない。
**あちらは Cookie も認証も持たない純関数**なので、クロスオリジンの問題が発生しない。

### なぜ GraphQL を使わないか

クライアントが1つしかないのに、スキーマ + codegen + Apollo の層を維持するコストが見合わない。
加えて GraphQL は**公開クエリ面を自分で守る必要がある**（深さ制限、複雑度制限、introspection、N+1）。
深さ制限や N+1 対策は入れれば済む話ではあるが、個人開発で払い続けたい税ではない。

### なぜ NoSQL より SQL か

決めた仕様が**そのまま DB 制約になる**。

- リスト数上限5つ → 制約でチェックできる
- 同じ項目の重複取り入れ防止 → ユニーク制約1行
- 取り入れ数ランキング → 集計クエリ1本

データ量は1ユーザー最大500項目で無料枠に対して極小。
**「間違ってもDBが止めてくれる」ことは、人間がレビューしない開発では安全側の資産**になる。

---

## 8. 認証

以下は 2026-08-05 時点の調査結果。

### 前提として確認したこと
- **Arctic（軽量 OAuth クライアント）は 2026年7月に非推奨化された。**
  移行方法は「リポジトリの `/code` にあるサンプルコードを自分の実装に取り込む」形。
  つまり「小さなライブラリに protocol 部分を任せる」という選択肢は消えた
- Cloudflare の `workers-oauth-provider` は、**Worker を OAuth プロバイダ（サーバー）にする**ためのもの。
  Google に対するクライアント用途ではないので今回は使えない

### 選択肢

| 案 | メリット | リスク |
|---|---|---|
| **Better Auth** ← **採用** | 設定がコードに乗る。DB セッションなので**失効が構造的に効く**。Hono + D1 + Drizzle の事例と専用ライブラリ（`better-auth-cloudflare`）がある。ベンダーが増えない | issue #4203 は**クローズ済み**。原因は `secondaryStorage`（KV）とクッキーキャッシュの併用時に KV からセッションを復元せず完全ログアウトしていたもの。**セッションストアを D1 のみにすればこの経路を踏まない** |
| Firebase Auth | 認証の中核を Google に外注できる。無料50k MAU、カード不要。Firestore を使わないので §6 D のリスクは負わない | 設定がコンソール管理（後述）。ベンダーが2つ。ID トークン検証コードが必要 |
| OAuth を自分で実装 | 依存が増えない、完全に明示的 | **採用しない。**セキュリティの中核を自分で持つことになる |

### Firebase Auth を「認証だけ」使う場合の注意

**構成のコード管理という観点では、部分的に問題がある。**

- **プロバイダ設定（Google の有効化、承認済みドメイン、サポートメール）はコンソールでしか設定できない。**
  `firebase.json` はホスティングやルールを扱うが、認証プロバイダ設定は対象外。
  Terraform で管理するには Identity Platform 側の API が必要で、それは Blaze（従量課金）前提になる。
  **無料枠のままではコード管理できない**
- 実害は費用ではなく保守。**コンソール設定が原因の不具合は、AI がコードを読んでも原因に到達できない。**
  「ログインできない」の原因が承認済みドメインの設定漏れだった場合、コードのどこにも手がかりがない
- 緩和策: **リポジトリにコンソール設定の手順と現在値を書き残す。**
  AI がそれを読んで「これはコンソール側の設定です」と指摘できる状態にしておく
- なお **Google の OAuth クライアント（リダイレクト URI）はどの案でもコンソール作業が必要。**
  Firebase Auth を選ぶと、その上に Firebase プロジェクト設定という2枚目のコンソール面が乗る、という差

**セキュリティ設計との噛み合わせにも注意が必要。**
Firebase の ID トークンは1時間で失効し、クライアント SDK が自動更新する。
つまり**サーバー側から即座に失効させられない**（Admin SDK の失効チェックが必要）。
これは §9 の「サーバー側から失効できるセッション」と衝突する。

> **解決策: ログイン時に1回だけ Firebase の ID トークンを検証し、以降は自前の DB セッション Cookie を使う。**
> Firebase の責務をログインの瞬間だけに閉じることで、失効が効き、毎リクエストが Firebase に依存しなくなる。

この形なら Firebase Auth を使っても設計原則を崩さない。採用する場合はこのパターンにする。

### `*.workers.dev` で運用する場合の注意

カスタムドメインを使わない（決定済み）。影響は3点。

- **WAF とレート制限ルールは使えない。**これらは Cloudflare のゾーン（自分のドメイン）に
  紐づく機能で、`workers.dev` には適用できない。
  → **レート制限は Workers の `ratelimit` バインディングで実装する**（§9）。
  ネットワークレベルの DDoS 対策は `workers.dev` でも効く
- **共有リンクが `<name>.<subdomain>.workers.dev` になる。**
  公開アプリとしての見た目の信頼感は落ちる
- ただし**後からカスタムドメインを足しても `workers.dev` のルートは残せる**ため、
  **すでに共有されたリンクは死なない。**共有が中心の機能である以上ここは重要で、
  「あとで独自ドメインにする」判断が安全に取れる

セッション Cookie は `Domain` 属性を付けず host-only にする
（`workers.dev` は他人のワーカーとサブドメインを共有する空間なので、これは必須）。

---

## 9. セキュリティ方針

要点は **「クライアントから DB を直接触らせない」**。
Firestore や Supabase の「クライアント直叩き + ルール」モデルは、ルールが唯一の防壁になる。
レビューする人間がいないなら、**認可をサーバーのコード1箇所に集める**方を選ぶ。

- 公開 URL の ID は推測不可能な値。連番 ID を露出させない
- 非公開判定は必ずサーバー側。**OGP 経路も同じ認可を通す**（画像だけ漏れるのが典型的な事故）
- **画像生成サービスに渡すデータは、呼び出し側で認可を通した後のものに限る。**
  生成サービス側に「このリストは公開か」を判断させない
- **画像生成エンドポイントは HMAC で署名する。誰でも叩ける状態にしない**
  （放置すると CPU 枠を溶かす踏み台になる）
- セッションは**サーバー側から失効できる**方式にする（トークンの検証だけで完結させない）
- 入力検証を Zod で1箇所に集約。文字数上限もここで効かせる
- 作成・取り入れ系にレート制限。**Workers の `ratelimit` バインディングを使う**
  （2025年9月に GA。ゾーン不要でカウンタはローカルにキャッシュされるためネットワーク待ちがない）。
  **計測期間は 10秒 か 60秒 のみ**なので、1日単位の上限が必要なら D1 のカウンタを併用する
- **画像に描くテキストは長さを制限する**（レイアウト崩れと CPU 消費の両方を防ぐ）
- シークレットは各プラットフォームの環境変数のみ。`.env` を git に入れない
  （**このリポジトリは public**。詳細は `docs/console-settings.md`）
- Dependabot で依存更新

---

## 10. AI が保守できる環境の整備

構成を決めたら、**先にこれを作る**。フレームワーク選択より効く。

- TypeScript を strict に。`any` を禁止する lint 設定
- DB スキーマから型を生成（Drizzle）し、入力は全て Zod で検証
- **ローカルで全部動く**こと（`wrangler dev` + ローカル D1）
- テストを最初から置く。特に**認可のテスト**を仕様として書く
  - 他人のリストが読めない／書けないこと
  - 非公開リストが OGP 経路から漏れないこと
  - リンク限定公開のリストの項目が取り入れ面に出ないこと
  - リスト数上限を超えられないこと
  - 署名のない画像生成リクエストが拒否されること
- テストは Vitest + Miniflare で D1 含めインメモリで回す
- `CLAUDE.md` に不変条件を書く（「認可は必ずこの層で」「DB に触るのはここだけ」など、
  コードから読み取りにくい約束事）
- **コンソールでしか設定できない項目の一覧と現在値をリポジトリに置く**（§8）
- **エラー通知を入れる**。コードを読まないなら、壊れたことを知る手段が別途必要

---

## 11. 決定事項

| 論点 | 決定 |
|---|---|
| OGP 画像の中身 | **リストのタイトルと達成状況**。項目一覧は載せない |
| ダウンロード画像の中身 | **すべてのやりたいこと**。**横長1種類**（約 1.41:1）、4列×25行のグリッド |
| ダウンロード画像の解像度 | **印刷は前提にしない。項目テキストが読めるサイズ**（目安 2000×1414） |
| OGP 画像の生成方式 | 動的生成。作り込む。将来カスタマイズ可能にする余地を残す |
| OGP のキャッシュ破棄 | **URL に更新日時を含める**（`/og/:id?v=<updatedAt>`） |
| 生成タイミング | 閲覧時ではなく**更新時**。手動更新も可 |
| 画像出力 | **OGP と同じパイプラインに載せる。ただしレイアウトは別テンプレート**（見た目は違う） |
| ラスタライズの実行場所 | **Deno Deploy の無料枠**。アプリ本体は Cloudflare Workers |
| OGP のカスタマイズ | **当面は決め打ち**。ユーザーが選べるようにはしない |
| 達成状況の見せ方 | **数値**（「23 / 100」） |
| 画像生成ライブラリ | **Satori + @resvg/resvg-wasm**（§4.3） |
| 日本語フォント | **フルセット同梱**。ウェイトは最小限、OFL のフォント（§4.2） |
| DB | SQL（D1）。NoSQL は採用しない |
| API 形式 | GraphQL を使わない |
| 認証 | **Better Auth**。セッションストアは **D1 のみ**（KV の `secondaryStorage` は使わない。§8） |
| Better Auth のバージョン | **`1.6.x` に固定する。1.7 系に上げない**（D1 のマイグレーションが通らない。§13） |
| ID の設計 | **編集用 `listId` と公開用 `shareId` を分ける**。どちらも推測不可能な値 |
| ドメイン | **カスタムドメインを使わない。`*.workers.dev` を使う**（注意点は §8） |
| リポジトリ | **`yaritai100list` を1つだけ作る。**デプロイ先は2つだがリポジトリは分けない |
| ホスト名 | `yaritai100list.<subdomain>.workers.dev` |
| 構成 | ¥0。行き詰まったら Cloudflare Workers Paid（$5）に寄せる |

---

## 12. 未決の論点

| # | 論点 | 備考 |
|---|---|---|
| 1 | OGP カードと画像出力の具体的なデザイン | 方向性は `PRODUCT_SPEC.md` §5.3 に記録。Satori の CSS 対応範囲（flexbox のサブセット、grid 不可）で成立する構成にする |
| 2 | `packages/shared` を Workers と Deno の両方から使えるか | 共有するのが型と Zod スキーマだけなら通るはず。詰まったら複製する方が安い |
| 3 | Better Auth 1.7 の D1 対応をいつ取り込むか | 1.7 は D1 のマイグレーションが通らない（§13）。upstream の修正待ち。**Dependabot が 1.7 に上げてこないよう固定する** |

> **解決済み:** 旧 #3「Deno Deploy の無料枠の現在値と規約」は 2026-08-06 に確認して片付いた（§13）。

---

## 13. 調査メモ（2026-08-06 再確認）

**この節の内容は変わる。着手時に再確認すること。**
初回調査は 2026-08-05。**2026-08-06 に #1 で再確認した結果を反映済み。**

### OGP の仕様
- SNS のカード表示は `og:image` と `og:title` / `og:description` を別々に扱う。
  タイトルや説明は画像に焼き込まなくてもテキストとして表示される
  （＝最小構成なら静的画像で成立する。§3 の退避先）
- **SNS 側は `og:image` を積極的にキャッシュする。**画像を更新しても URL が同じだと反映されない。
  URL にバージョンを含めるのが定石

### Deno Deploy（2026-08-06 再確認済み）
- 無料枠を「1リクエストあたり 50ms CPU」から**月間 15 CPU 時間のプール制**に変更。
  これにより無料枠で画像生成が可能になった
- 公式価格ページの無料枠: 月100万リクエスト / **CPU 15時間 / 帯域 20GB** /
  KV 1GiB / アプリ20個まで / 単一リージョン / メンバー5人。Pro は $20/月
- **帯域は 20GB が正。**記事の 100GB は誤りか古い値（2026-08-06 に公式価格ページで確認）。
  OGP 画像は Cloudflare CDN でキャッシュして配るので 20GB でも足りる
- **商用利用の制限は見つからない。**現行の価格ページにも Usage Guidelines にも
  「personal use」限定の文言はない（以前あった "personal use and smaller projects" の表現は現在ない）
- **クレジットカード登録は不要。**サインアップ時に求められないことを確認（2026-08-06、#1）。
  「登録しないと制限が厳しくなる」という記述を見かけたが、現在の画面では確認できなかった
- リージョンは US / EU のみ。日本にない
- Classic（dash.deno.com）は 2026年7月20日に廃止。新プラットフォームは console.deno.com
- **リソースはすべて organization 所有。**org は最低1つ必要
- **デフォルトドメインは `{app-name}.{org-slug}.deno.net`。**
  org slug は後から変更できるが、変更すると既存 URL が全部壊れる（TLS 再発行に数分）
- ビルド時間も CPU 枠を消費する

### 認証ライブラリ（2026-08-06 再確認済み）
- **Arctic は 2026年7月に非推奨化。** 移行はサンプルコードの取り込み
- Cloudflare `workers-oauth-provider` は Worker を OAuth **プロバイダ**にするためのもので、用途が違う
- **Better Auth** は Hono + D1 + Drizzle の事例あり。`better-auth-cloudflare` という統合ライブラリも存在
- **issue #4203 は 2026-04-23 に completed でクローズ済み。**
  加えて、原因だった `secondaryStorage`（KV）を使わない構成なのでこの経路自体を踏まない
- バージョン（2026-08-06 時点）:
  `better-auth@1.6.26`（`latest`、2026-08-05 公開＝活発）/
  `better-auth-cloudflare@0.3.1`（2026-07-23、★570、アーカイブされていない）
- 1.7 系は現在 `1.7.0-rc.4` / `1.7.0-beta.10`。**まだ GA していない**

#### ⚠️ Better Auth 1.7 は D1 で使えない（未修正）

**[issue #10551](https://github.com/better-auth/better-auth/issues/10551)（2026-07-27 起票、オープンのまま）**

1.7 で `getMigrations()` に追加された `getDatabaseIndexes()` が
`pragma_index_list` / `pragma_index_info` を使う。**D1 の authorizer は Worker バインディング経由での
pragma テーブル値関数を拒否する**ため `D1_ERROR: not authorized: SQLITE_AUTH` になる。
マイグレーションがテーブル作成前に落ちるので、**1.7 では D1 にスキーマを作れない。**

- 1.6 系は `getDatabaseIndexes()` が存在しないので影響を受けない
- **`wrangler d1 execute` からは再現しない。**CLI とバインディングで authorizer の厳しさが違うため、
  Worker 内でしか再現しない。ローカルで通っても本番で落ちる型の不具合
- 対処: **`better-auth` を `1.6.x` に固定する**（§11 の決定事項）。
  **Dependabot が 1.7 に上げてこないよう設定する**。upstream の修正を確認してから上げる

#### その他の関連する既知の問題（いずれもオープン）

- [#10315](https://github.com/better-auth/better-auth/issues/10315):
  初期化リクエストが abort されると、遅延キャッシュされた init promise が Workers 上で永久にハングする
- [#10566](https://github.com/better-auth/better-auth/issues/10566):
  `customSession` が内部の `getSession` エラーを `null` に潰すため、
  インフラ障害と「未ログイン」が区別できない。**エラー通知の観点で注意**

### 画像生成ライブラリ（2026-08-06 再確認済み）
- `satori@0.29.0`（2026-07-23、MPL-2.0）/ `@resvg/resvg-wasm@2.6.2`（2026-01-28、MPL-2.0）
- どちらもメンテされており、Deno Deploy / Cloudflare Workers での利用例がある
- **ライセンスは MPL-2.0。**ファイル単位のコピーレフトなので、
  ライブラリとして利用する限り自作コードの開示義務は生じない（改変して再配布する場合は当該ファイルのみ対象）

### 出典
- https://deno.com/deploy/pricing
- https://docs.deno.com/deploy/usage/
- https://docs.deno.com/deploy/reference/organizations/
- https://docs.deno.com/deploy/reference/domains/
- https://zenn.dev/sora_kumo/articles/deno-ogp-image
- https://arcticjs.dev/
- https://github.com/better-auth/better-auth/issues/4203
- https://github.com/better-auth/better-auth/issues/10551
- https://github.com/zpg6/better-auth-cloudflare
- https://github.com/cloudflare/workers-oauth-provider
