-- 取り入れの記録をやめる（親 #10、2026-08-10 の利用者の判断）。
--
-- 🔴 **「全文一致数」と「取り入れ数」を区別しない**ことにした。突き詰めると
-- この表の使い道が無くなる。
--
-- 取り入れると自分のリストにその本文の項目が1つ増える。**取り入れは全文一致数に
-- 自動的に現れる**ので、別に数える必要がない。
--
-- 0008（表）と 0009（本文を書き換えたら消すトリガー）を戻す。
-- **入れた直後だが、消せる量が多いので戻す**（取り入れ API も、
-- 「プールに無い本文を送られて指標を水増しされる」問題ごと消える）。
--
-- ⚠️ **トリガーを先に落とす。** 表を落としてもトリガーは残り、
-- 次に `items` の本文を更新したときに「no such table: adoptions」で失敗する。
DROP TRIGGER IF EXISTS adoptions_drop_on_text_change;--> statement-breakpoint
DROP TABLE IF EXISTS `adoptions`;
