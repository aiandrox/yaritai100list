# やりたいことリスト100 — プロダクト仕様

プロダクト仕様。技術スタックは扱わない（`TECH_STACK.md`）。

---

## 1. コアコンセプト

**やりたいことを書き溜めて、叶えたら印を付けていくアプリ。**

- 思い立った瞬間に書けること。ログインも初期設定も要求しない。
- 100個という枠が「まだ埋まっていない」という動機を生む。埋める楽しさと、消化する楽しさの両方がある。
- 書いたものを人に見せられる。他人のやりたいことを見て「それいいな」と自分のリストに取り込める。

3つ目が将来的な軸で、これがあるかないかで設計が変わるため、MVP時点から前提として置いておく。

---

## 2. 利用者の状態と機能差

| | 未ログイン | ログイン |
|---|---|---|
| リストの保存先 | ブラウザのみ | サーバー（永続） |
| 端末間で同じリストを見る | できない | できる |
| 持てるリスト数 | 1つだけ | 複数（マイリスト、**最大5つ**） |
| 項目の追加・編集・削除 | できる | できる |
| **項目を完了にする** | **できない**（2026-08-07 決定） | できる |
| リストの共有（将来） | できない | できる |
| 他人のリストを見る（将来） | できる（閲覧のみ） | できる |
| 他人の項目を取り入れる（将来） | できない | できる |
| 画像出力（将来） | できる | できる |

方針として、**未ログインでも「使える」が「失う可能性がある」**という状態にする。
ログインの動機は「消えない」「複数持てる」「人に見せられる」「『やった』印を付けられる」の4つ。

### 完了はログインが要る（2026-08-07 決定）

**書くのは自由、「やった」印を付けるのはログインしてから。**

書き始めるまでの障壁は作らない（§1）が、完了は「後から見返して意味を持つ記録」であり、
ブラウザにしか無い状態で貯めても消える。**消えて困るものを貯め始める地点をログインに合わせる。**

- **完了の取り消しも同様にできない。** 片方だけ許すと状態が捻れる
- 隠さずに見せて、**押したら『Googleでログイン すると、「やった」印を付けられます』と出す。**
  できないことを黙って無効化すると、機能が無いのか壊れているのか区別が付かない
- **案内は行の上に重ねる。** 行の間に差し込むとリスト全体が押し下がり、
  100行が一斉に動いて何が起きたのか分からなくなる
- 既に `completedAt` が入っている項目（ログイン中に付けた印）は、そのまま完了として表示する

### ログイン時の引き継ぎ

未ログインで書いたリストは、ログイン時に**マイリストの1つ目として取り込む**。

- 取り込んだらブラウザ側のデータは消す（二重管理を避ける）
- すでにマイリストを持っているユーザーが未ログイン状態で書いた場合は、**新しいリストとして追加**する。
  既存リストへのマージはしない（意図しない混ざりを防ぐ）
- 空のまま（1項目も書いていない）ならば何も取り込まない
- **リスト数上限（5つ）に達している場合は取り込まない。**ブラウザ側のデータは消さずに残し、
  「取り込めていない」ことを伝える。リストを整理すれば取り込める導線にする
  （勝手に上限を超えさせない／勝手に捨てない）

---

## 3. 概念モデル

### ユーザー
- 認証はソーシャルログイン（プロバイダは技術検討で決める）
- **人に見せる名前を持たない。** 公開はリスト単位で行われ、どの公開面にも作者は表示されない（§5.1）。
  そのためプロフィールや表示名の設定を持たない
- **名前とメールアドレスは DB に保存するが、どこにも表示しない**（2026-08-06 決定）。
  Better Auth の既定スキーマが `user.name` と `user.email` を必須で持つため、
  削るより「保存はするが使わない」を制約として守る方が壊れにくい。
  - 画面にも公開面にも出さない（作者を表示しないという決定がそのまま効く）
  - **エラー通知にも送らない。** Sentry に送るのは `user.id`（このサービス上の一意キー）だけ
- **セッションに IP と User-Agent を保存しない**（2026-08-06 決定、#58）。
  ログイン履歴も端末一覧も作らないので読む機能が無い。
  ⚠️ **IP の取得自体は止めない。** 止めるとレート制限のキーからも IP が消える（`src/auth.ts`）
- **OAuth の資格情報は保存しない**（2026-08-06 決定、#58）。
  Google は**ログインの手段としてしか使わない**ので、`access_token` / `id_token` /
  `refresh_token` に使い道が無い。使わないものを持ち続けない、という判断。
  実装は `src/auth.ts` の `databaseHooks`（Better Auth に「保存しない」設定が無いため）

### リスト
- タイトルを持つ（既定「人生でやりたいことリスト」）。
  サービス名「やりたいことリスト100」と画面上で紛れないよう、頭に「人生で」を付ける
- 項目を持つ
- 公開設定を持つ（後述）
- 1ユーザーが複数持てる。「2026年にやりたいこと」「一生のうちにやりたいこと」のように使い分ける

### 項目（やりたいこと）
- 本文（短い1行）
- 完了状態。**完了日時を持つ**（いつ叶えたかは思い出として意味があるため、真偽値にしない）
- リスト内での並び順
- 取り込み元（将来。他人の項目から取り入れた場合に記録する）

### 100という数
**上限かつ目標として扱い、項目の実体は可変長にする。**

- 表示は「23 / 100」のように達成度ではなく埋まり具合を示す
- 100枠を常に空行で描画するのは MVP の表示方式として維持してよいが、
  データとして100個の空スロットを持つ設計にはしない
- 理由: 複数マイリストを許すと「20個で完結するリスト」が普通に発生する。
  また「他人の項目を取り入れる」は末尾追加になるため、固定スロットと相性が悪い

### 番号
固定スロット番号ではなく**並び順**とする。並び替え・削除時の詰め直しができるようにする。
表示上は `001` から連番を振る。

---

## 4. MVP スコープ

### 4.1 未ログインで書く
- アクセスするとすぐリストが表示され、その場で入力できる
- 入力内容はブラウザに保存され、再訪時に復元される
- 「保存されていない可能性がある」ことを伝える導線を置く（ログインの動機付け）
- **完了にはできない**（§2）。押したら「Googleでログイン すると、「やった」印を付けられます」と出す

### 4.2 ログイン / ログアウト
- ソーシャルログイン1種類でよい
- **ログアウトできること。** サーバー側からセッションを失効させる（`TECH_STACK.md` §9）

### 4.3 マイリスト管理

**画面での呼び名は「すべてのリスト」**（2026-08-07 決定、#110）。
仕様上の概念名は「マイリスト」だが、画面には出さない。
- リストの一覧表示
- 作成 / タイトル変更 / 削除
- リストの切り替え
- 削除時は確認を挟む（項目ごと消えるため）

**画面遷移**: トップを開いたときは**最後に更新したリストを直接表示する**。
リストを1つしか持っていない人に無駄な操作を作らないため。
一覧画面は切り替え・管理のために別途用意する。

**一覧の並びは作った順**（2026-08-07 決定、#108）。
更新順にすると、タイトルを直したり項目を書いたりするたびに**そのリストが先頭へ飛び、
どれを触ったか見失う。** 「完了しても項目の位置は動かさない」（§4.5）のと同じ理由。
トップが「最後に更新したリスト」を開くのは**1つを選ぶ基準**であって、並べる基準ではない。

### 4.4 項目の編集
- 本文の入力・変更
- 完了にする / 完了を取り消す（**ログインが要る**。§2）
- 削除
- 並び替え（MVPでは省略可。ただしデータ設計は並び順前提にしておく）

### 4.5 表示
- モバイル縦1カラムを主対象とする
- 完了した項目が視覚的にわかること

---

## 5. 将来対応したい機能

順序は「共有 → OGP → 画像出力 → 取り入れ」を想定。共有が全ての前提になる。

### 5.1 リストの共有（全公開 / リンクで限定公開）

公開範囲を3段階で持つ。**公開設定はリスト単位のプロパティ**であり、アカウントには公開に関する設定を持たない。

| 設定 | リストを見られる人 | 項目が取り入れ面のプールに入る |
|---|---|---|
| 非公開 | 自分だけ | 入らない |
| リンク限定公開 | URLを知っている人 | 入らない |
| 全公開 | URLを知っている人 | **入る** |

3段階だが、リストの見え方は「非公開」か「URLを知っていれば見える」の2値しかない。
**全公開とリンク限定公開の差は、項目を取り入れ面に提供するかどうかだけ**である。
リストそのものが一覧・検索に並ぶことはない（並ぶのは項目のプール）。

- 既定は**非公開**。公開は明示的な操作でのみ起きる（うっかり公開される経路を作らない）
- 共有URLは推測不可能なIDを使う（連番IDを露出させない）
- **共有リンクを再発行できる。** 公開用のIDは編集用のIDと別に持ち、再発行すると古いリンクが無効になる。
  「うっかり送った相手に見られ続ける」状態から抜ける手段として用意する
- 公開されるのは**常に最新の状態**。スナップショットは取らない
- 公開ページは読み取り専用。編集UIを出さない

#### 公開面は2種類あり、見せる情報が異なる

| | 共有公開ページ | 取り入れ用の公開面 |
|---|---|---|
| 目的 | 自分のリストを人に見せる | やりたいことのアイデアを探す |
| 見せ方の単位 | リスト1件 | やりたいこと（項目） |
| 到達経路 | 共有URL | サービス内の一覧・検索 |
| リストのタイトル | 表示する | — |
| 作者 | **表示しない** | **表示しない** |
| **完了マーク** | **表示する** | **表示しない** |
| **完了日時** | **表示する** | **表示しない** |
| 対象となる公開設定 | リンク限定公開 / 全公開 | 全公開のみ |

共有公開ページは「私はこれをやりたくて、これは叶えた」という自己表現なので、達成状況が主役になる。
取り入れ用の公開面は**アイデアそのものを探す場**なので、叶えたかどうかは削ぎ落とす。

**どちらの面にも作者を表示しない。** 共有公開ページに来るのは URL を渡された人なので、
誰のリストかは文脈から分かっており、アプリが名乗る必要がない。
取り入れ面はアイデアのプールなので、そもそも人に紐付けない。

この設計から3つ帰結する。

- **アカウントに公開用の名前・プロフィールを持たない。**設定画面が1つ減り、
  ソーシャルログインの本名が漏れる経路も生まれない
- 取り入れ面は「他人のリストを見るページ」ではなく、**やりたいことが集まったプール**として設計する。
  リスト単位でのブラウズが主動線ではない
- 取り込み元の記録は**内部的な指標**として持つ。ユーザーに見えるのは「誰の」ではなく
  「どれだけ取り入れられているか」だけになる

将来、共有公開ページ側で完了日時を隠したい需要が出た場合はオプションとして足せる余地を残す
（ただし MVP 後の初期実装では表示する）。

**今決めておくべきこと**: 公開範囲はリスト単位であり、項目単位の公開/非公開は持たない。
「1項目だけ隠す」需要が出たら別リストに分ける運用にする。

### 5.2 OGP でやりたいこと一覧を表示

共有URLを SNS に貼ったときに、リストの中身が画像として見える。

**SNS カードは共有時に目を引く要素であり、サービスへの入口になるため作り込む。**
タイトルだけならメタタグのテキストで表示されるので画像は静的でも成立するが、
見栄えを取って動的に生成する。

**載せるのはリストのタイトルと達成状況。やりたいことの一覧は載せない。**
共有された相手に「何のリストで、どこまで進んでいるか」が一目で伝わればよい。
一覧を見せるのは画像出力（§5.3）の役割で、**2つは別のデザインになる**。

- タイトルと達成数（23/100）を入れる。作者は入れない（§5.1）
- 達成状況の見せ方（数値か進捗バーか）は未決 → §7
- **将来ユーザーがカスタマイズできる余地を残す**（配色など）。
  何をカスタマイズ可能にするかは未決 → §7
- **更新の反映方法**: 生成は閲覧時ではなく更新時に行う。
  SNS 側が画像を強くキャッシュするため、画像の URL に更新日時を含めて別 URL として扱わせる。
  これをやらないと「更新したのに古い画像が出る」状態になる
- 非公開リストの OGP は生成しない

### 5.3 画像出力

リストを画像として保存し、SNS等に自分で投稿できる。

**載せるのはすべてのやりたいこと。**OGP（§5.2）とは中身も見た目も別で、こちらが一覧を見せる役割。

**フォーマットは横長1種類のみ。**比率は A4 横に近い（約 1.41:1）。

### レイアウト

参考にするのは [nolty「1年でやりたい100のコト」](https://nolty.jp/100nokoto/output/) の構成。
**借りるのはレイアウトの構造だけで、配色・イラスト・ロゴはこちらで用意する。**

- **4列 × 25行のグリッド**に 001〜100 を並べる
- **未入力の番号も表示する。**空欄のまま100個すべて並べることで「まだ埋まっていない」が見える。
  アプリ画面で100行を必ず描画するのと同じ思想（§4.5）
- **番号は3桁ゼロ埋め**（`001`）。太めの数字で、項目テキストより目立たせる
- 項目テキストは番号の右に小さめに置く
- 見出し（リストのタイトル）は左上に大きく
- サービス名は右上
- 背景は単色。余白を広く取る

### その他

- 完了状態を表現に含める
- 未ログインでも使えるようにする（拡散の入口になる）
- OGP と**ラスタライズの仕組みは共有できる**（フォントの用意など）。
  **先に OGP を作ると画像出力が安く済む**。ただしレイアウトと縦横比は別
  （OGP は 1200×630 の 1.91:1、こちらは 1.41:1）

### 5.4 他の人のやりたいことを自分のリストに取り入れる

公開されているリストを見て、気に入った項目を自分のリストに追加する。

- **取り入れの単位は項目**（リスト丸ごとのコピーではない）
- 取り入れた項目は自分のリストの末尾に追加され、以降は自分のものとして編集・完了できる。
  完了状態は引き継がない（他人が叶えたことは自分の達成ではない）
- **取り込み元を記録する**。これがあると「何回取り入れられたか」を人気指標として出せる。
  後から遡って付けられないため、この機能を作る時点で記録を始める必要がある
- 取り入れ面は §5.1 の通り**項目のプール**として見せる。作者・完了状態は出さない。
  入口は「よく取り入れられているやりたいこと」のような並びを想定する
- **プールの項目から元のリストへは辿れない。** 出典リンクを張らない。
  項目は書いた人から切り離されたアイデアとして流通する。
  これにより取り入れ面と共有公開ページは経路として交わらず、
  「見知らぬ人が他人のリストに着地する」状況が発生しない（＝作者を名乗る必要が生じない）
- 同じ項目を重複して取り入れられないようにする（既に自分のリストにあるものは取り入れ済みと分かる）
- ログイン必須（保存先が必要なため）

---

## 6. 対象外（今回はやらない）

- フォロー・コメント・いいね等の SNS 機能
- リストの共同編集
- 期限、リマインダー、通知
- 項目へのメモ・写真・URL の添付
- カテゴリ / タグ
- 達成の記録（写真や感想を残す）

「取り入れ」以外の対人機能を持ち込まないことで、リストアプリとしての単純さを保つ。

---

## 7. 判断状況

### 決定済み
| 論点 | 決定 |
|---|---|
| 共有公開ページに完了情報を出すか | **出す**（完了マーク・完了日時とも） |
| 取り入れ用の公開面に完了情報を出すか | **出さない**。項目のみ |
| 取り入れの単位 | 項目単位。リスト丸ごとコピーは持たない |
| 公開時の作者表示 | **どの公開面にも出さない**。よってユーザーは表示名を持たない |
| 公開設定の所在 | リスト単位のプロパティ。アカウント側に公開設定を持たない |
| 取り入れ面から元リストへのリンク | **張らない**。2つの公開面は経路として交わらない |
| 共有リンクの再発行 | **できる**。公開用IDを作り直すと古いリンクが無効になる |
| OGP に載せる情報 | **リストのタイトルと達成状況のみ**。項目一覧は載せない（一覧は画像出力の役割） |
| OGP の達成状況の表示 | **数値**（「23 / 100」） |
| OGP のカスタマイズ | **当面は決め打ち**。ユーザーが配色などを選べるようにはしない |
| ダウンロード画像のフォーマット | **横長1種類のみ**（約 1.41:1）。縦長・正方形・A4 の出し分けはしない |
| ダウンロード画像のレイアウト | **4列×25行に 001〜100 を全部並べる**。未入力の番号も空欄で表示 |
| ダウンロード画像の解像度 | **印刷は前提にしない。ただし項目テキストが読めるサイズ**（目安 2000×1414） |
| 配色 | **ピンク**（`#ffa2ab`） |
| 日本語のサービス名 | **やりたいことリスト100** |
| 英語名・リポジトリ名 | **`yaritai100list`**。新しい GitHub リポジトリを1つ作る |
| トップを開いたときの表示 | **最後に更新したリスト**（一覧ではない） |
| リストの既定タイトル | **「人生でやりたいことリスト」**。サービス名「やりたいことリスト100」と画面上で紛れないよう「人生で」を付ける |
| 未ログインで項目を完了にできるか | **できない**（2026-08-07 決定、#77）。完了はログインの動機にする。取り消しも同様にできない（§2） |

### 暫定（推奨案のまま進める。異論があれば差し戻す）
| # | 論点 | 暫定 |
|---|---|---|
| 1 | 100は固定枠か、上限・目標か | **上限・目標**。実体は可変長（§3） |
| 2 | 「全公開」で一覧・検索に載るのは何か | **リストではなく項目**。全公開の効果は項目をプールに提供すること |
| 3 | 未ログインで持てるリストは1つでよいか | よい。複数はログインの動機として残す |
| 4 | 完了した項目をリスト内で下に移動させるか | させない（番号＝並び順が動くと混乱する） |
| 5 | リスト数の上限 | **1ユーザー5つまで**（とりあえずの値。運用を見て見直す） |
| 7 | 項目本文の文字数上限 | **22文字**（旧実装と同じ）。画像出力のレイアウトを見て見直す（#9） |
| 8 | リストタイトルの文字数上限 | **15文字**（旧実装と同じ）。同様に見直す |

上限値は `packages/shared/src/limits.ts` にあり、Zod で1箇所で効かせている。
Zod のスキーマを変えるだけで済むので、実装が進んでから決め直せる。

### 未決（後で決めればよい）
| # | 論点 | 備考 |
|---|---|---|
| 9 | 取り入れ面の並び順・入口の具体 | 「よく取り入れられている」以外の軸を持つか |
