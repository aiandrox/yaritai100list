# MEMO

`PRODUCT_SPEC.md` / `TECH_STACK.md` に収まらない申し送り事項。
検討時期: 2026-08-05 〜 2026-08-06。

---

## 1. 旧環境の後始末（未実施）

作り直しにあたって捨てる／片付ける対象。**まだ何もしていない。**

- [ ] **Google OAuth のクレデンシャルをローテートする。**
      旧リポジトリの git 履歴に `GOOGLE_CLIENT_SECRET` と `NEXTAUTH_SECRET` が残っている。
      コミット `735eed0` で `.env` から削除されているが、履歴からは辿れる。
      鍵が生きているなら Google Cloud Console で無効化する
- [ ] GitHub の旧リポジトリをアーカイブする
  - `aiandrox/yaritaikoto100`（Next.js のフロント。ローカルでは `yaritaikoto100_fe2`）
  - `aiandrox/yaritaikoto100_be`（Rails）
- [ ] Vercel のプロジェクト（`yaritaikoto100.vercel.app`）を停止するか、新サービスへの案内に差し替える
- [ ] ローカルの `~/private/development/yaritaikoto100/`（`_be` / `_fe` / `_fe2` を含む）を破棄する
      - 旧ローカルには未コミットの変更が残っていたが、捨てる判断のため退避しない
      - `_fe` は 2024-01-02 に始めた Next.js → CRA 移植の中断物。リモートなし。デプロイもされていない

## 2. 旧アプリの利用者データは引き継げない

**これは判断が必要な事項。**

- 旧アプリは BE に繋がっておらず（詳細は `docs/legacy-spec.md`）、
  データは**各利用者のブラウザの localStorage にのみ存在する**（キー `items`）
- **ドメインが変わると localStorage は読めない。**
  `yaritaikoto100.vercel.app` に保存されたデータを、新しいホストから取得する方法はない
- したがって**既存利用者のデータは移行不能**
- 利用者がどれだけいるかは不明。旧ドメインに「新サービスへ」の案内を置くかどうかは要判断

## 3. 着手前に用意が必要なもの

- Cloudflare アカウント（Workers / D1）
- Deno Deploy アカウント（画像生成用）。
  **クレジットカードを登録しないと制限が厳しくなる**との情報がある
- Google Cloud の OAuth クライアント（**新規に作る**。リダイレクト URI は新しいホスト名）
- エラー通知先（Sentry の無料枠など）。
  コードを人間が読まない前提なので、**壊れたことを知る手段は必須**

## 4. 実装の進め方（提案）

**土台 → 縦に1本通す → 機能を足す**、の順を推す。
人間がコードをレビューしないため、**テストと型が唯一の安全網**になる。
機能より先に検証環境を作る方が、後から入れるより確実に安い。

1. **土台**: TypeScript strict / Drizzle / Zod / `wrangler dev` でローカル起動 /
   Vitest + Miniflare（D1 込み）/ CI
2. **認証を通す**（Better Auth + D1 セッション）。**認可のテストをここで書く**
3. 未ログインのリスト編集（localStorage）
4. ログイン後の永続化 + localStorage からの引き継ぎ
5. マイリスト（最大5）

ここまでが MVP。以降は将来機能。

6. 共有公開ページ
7. OGP 画像（Deno Deploy 側の実装はここで初めて必要になる）
8. ダウンロード画像
9. 取り入れ面

**7 を先に作ると 8 が安く済む**（フォントとラスタライズの仕組みを共有するため）。

## 5. 命名の検討過程

最終: **`yaritai100list`**（英語名・リポジトリ名）/ **やりたいことリスト100**（日本語名）

検討した候補と見送った理由。

| 候補 | 見送った理由 |
|---|---|
| `hyaku` / `cento` | 短くて良いが、意味が伝わる範囲が狭い |
| `kanae`（叶え） | 情緒的に強いが、100 という枠がコンセプトの核なので落としたくない |
| `wishlist` | **英語では「ほしいもの」の意味が支配的。**体験を書くこのアプリとは意味がズレる |
| `bucketlist` 系 | 意味は正確。ただし技術文脈の bucket（R2 / S3）と紛らわしい |
| `hundred-bucketlist` | 長く、英語としてやや不自然（冠詞なしの hundred） |

## 6. その他

- **無料枠やライブラリの状況は 2026-08-05 時点の調査。**
  着手時に `TECH_STACK.md` §13 を必ず再確認する
- 既存の `~/private/development/og-image-generator` は create-next-app の雛形のみで、
  再利用できるものはない（Satori も resvg も入っていない）
- 旧実装の `SPEC.md`（棚卸し）は `docs/legacy-spec.md` として持ち込んでいる
