node_modules/
dist/
build/
.output/

# Cloudflare
.wrangler/
.dev.vars
.dev.vars.*
# ひな形は値を持たないのでコミットする（変数名を discoverable にするため）
!.dev.vars.example

# wrangler types が生成する Workers ランタイムの型（550KB 超）。
# コミットすると検索のノイズになり、wrangler を更新したときに古い型が黙って残る。
# typecheck の前に毎回生成する（apps/web の pretypecheck）。
worker-configuration.d.ts

# 環境変数・秘密情報（旧実装では .env を履歴に入れてしまっていた）
.env
.env.*
!.env.example

# ローカル DB
*.sqlite
*.sqlite3
*.db

# テンプレートを目で確かめるための出力（apps/render の deno task preview）
apps/render/preview-*.png

# テスト・カバレッジ
coverage/

# Claude Code（settings.json は共有する。local は個人の許可設定なので入れない）
.claude/settings.local.json

# エディタ・OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log
